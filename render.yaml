services:
  - type: web
    name: cpc-report-monthly-nocomment
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8080
